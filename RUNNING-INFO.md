# Running Info

The completed project is running at:

http://127.0.0.1:8000/

## Login Details

- Admin: `admin@example.com` / `password`
- Editor: `editor@example.com` / `password`
- User: `user@example.com` / `password`

## Notes

- The original MySQL schema is still included at `Task-5_Final_Project/blog-app/database/schema.sql`.
- Your local MySQL service is running, but it requires a root password. The Desktop running copy automatically falls back to a local SQLite database so the project opens and works immediately.
- To switch back to MySQL, update `Task-5_Final_Project/blog-app/config/database.php` with your MySQL username and password, then import `schema.sql`.

